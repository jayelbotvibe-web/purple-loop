# DESIGN.md — placeholder

The canonical architecture document was delivered separately (the HTML/`DESIGN.md`
architecture plan). **Replace this file with that full content** before kickoff —
the agent reads `DESIGN.md` as the source of truth for the architecture.
