#!/usr/bin/env bash
# The full backlog-creation script lives in AGENT_PLAYBOOK.md, section 5.
# Copy it here verbatim (it is kept in the playbook so the plan stays in one
# reviewable place), set R= to your repo, review, then run.
echo "See AGENT_PLAYBOOK.md section 5 for the issue-bootstrap script." >&2
exit 1
